/** @type {import('next').NextConfig} */
const nextConfig = {
  // 本番ビルドで ESLint/TS エラーでは落とさない（公開を優先）
  eslint: { ignoreDuringBuilds: true },
  typescript: { ignoreBuildErrors: true },
};

export default nextConfig;
